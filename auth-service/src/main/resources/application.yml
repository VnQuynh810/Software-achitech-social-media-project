spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
      javax:
        persistence:
          validation:
            mode: none
    hibernate:
      ddl-auto: update
    show-sql: true


  datasource:
    url: jdbc:mysql://localhost:3301/authService?useSSL=false&serverTimezone=UTC
    username: root
    password: nguyen2005

server:
  port: 8081

jwt:
  secret: "mysupersecretkeymysupersecretkeymysupersecretkey"

app:
  services:
    profile: http://localhost:8083/profile